name: Update Sankey Dashboards

on:
  schedule:
    # Every Monday at 8am UTC (adjust for your timezone)
    - cron: '0 8 * * 1'
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Fetch HubSpot data and rebuild dashboard
        env:
          HUBSPOT_API_KEY: ${{ secrets.HUBSPOT_API_KEY }}
        run: python scripts/fetch_and_build.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html deals.html
          git diff --staged --quiet || (git commit -m "Update dashboard data $(date +%Y-%m-%d)" && git push)
